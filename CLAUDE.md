@AGENTS.md

# 개발 워크플로 원칙

## 개발 전
1. **설계 먼저** — 기능 구현 전에 설계(구조, 타입, UI/UX, 이동 경로)를 문서화한다
2. **Advisor 리뷰** — 설계 완료 후 `advisor()` 를 호출해 리뷰를 받는다
3. **사용자 결정** — Advisor 피드백 중 사용자가 선택해야 할 항목을 정리해 확인받는다
4. **승인 후 구현** — 사용자 승인 없이 구현을 시작하지 않는다

## 개발 후
1. **Advisor 리뷰** — 구현 완료 후 `advisor()` 를 호출해 최종 리뷰를 받는다
2. **빌드 확인** — 반드시 `npm run build` 로 빌드 성공을 검증한다
3. **배포** — 사용자 승인 후 `npm run deploy` 로 배포한다
4. **커밋** — 배포 후 변경 내용을 커밋한다

## 기타 원칙
- 복잡한 기능은 설계→리뷰→구현→리뷰 사이클을 반드시 거친다
- 간단한 수정(오타, 오류 수정 등)은 설계 단계 생략 가능
- Advisor 리뷰 없이 대규모 구현을 시작하지 않는다
